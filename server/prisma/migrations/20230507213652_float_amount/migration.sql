BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[basket] ALTER COLUMN [BasketAmount] FLOAT(53) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
